// Выбор цвета
/*var _color = textColor;
if (isClicked) {
    _color = clickedColor;
} else if (hover) {
    _color = hoverColor;
}*/

draw_set_font(fnt_komi);

draw_text_ext_color(x, y, text, sep, w, textColor, textColor, textColor, textColor, 1);